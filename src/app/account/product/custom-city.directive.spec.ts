import { CustomCityDirective } from './custom-city.directive';

describe('CustomCityDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomCityDirective();
    expect(directive).toBeTruthy();
  });
});
